MEMORY {
    MAIN: start = $1900, size = $1000, file = %O, fill = no;
    STUBR: start = $0900, size = $0400, file = "", fill = no;
}
SEGMENTS { CODE: load = MAIN, type = ro;  STUB: load = MAIN, run = STUBR, type = ro; }
