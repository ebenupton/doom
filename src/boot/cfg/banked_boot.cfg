MEMORY {
    B: start = $0900, size = $0600, file = "%O.boot", fill = no;
    D: start = $0F00, size = $0600, file = "%O.drv",  fill = no;
}
SEGMENTS { BOOTLD: load = B, type = ro;  DRV: load = D, type = ro; }
