# slope_div (angle module) — BANKED build for the Model B port.
# Code at $3400 (clipper-vacated low RAM); BCA workspace at $3A00 bounds it.
MEMORY {
    ANG_BK: start = $3400, size = $0600, file = %O;
}
SEGMENTS {
    ANG_BK: load = ANG_BK, type = ro;
}
