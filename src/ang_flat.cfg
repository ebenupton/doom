# slope_div (angle module) — flat build. Mirrors beebasm SAVE:
#   bsp_render_ang.bin @ $E940, must not grow into TA_HI at $F200.
MEMORY {
    ANG: start = $E940, size = $08C0, file = %O;
}
SEGMENTS {
    ANG: load = ANG, type = ro;
}
