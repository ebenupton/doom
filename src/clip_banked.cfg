# span_clip — BANKED build @ $8000 (span_clip_bankc.bin, sideways bank C).
# No umul8 pin in the banked build; code flows from the jump table.
MEMORY {
    CLIP_BK: start = $8000, size = $4000, file = %O;
}
SEGMENTS {
    CLIP_BK: load = CLIP_BK, type = ro;
}
