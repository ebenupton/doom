; ============================================================================
; bsp/main_tail.s — historical home of the end-of-code markers; they
; live in bsp/zzz_end.s now (included LAST so the marker is
; STRUCTURALLY last — the ZZTAIL fall-through landmine class is dead).
; ============================================================================
SEG_CODE
